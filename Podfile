platform :ios, '8.0'
inhibit_all_warnings!

xcodeproj 'Hoot'

plugin 'cocoapods-keys', {
    :project => "Hoot",
    :keys => [
        "ParseAppID",
        "ParseClientKey"
    ]
}

pod 'Parse', '~> 1.8'
